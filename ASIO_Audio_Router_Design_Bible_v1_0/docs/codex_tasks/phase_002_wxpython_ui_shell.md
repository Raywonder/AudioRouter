# Phase 002 Wxpython Ui Shell

Create accessible wxPython UI shell with menus, status console, and command palette placeholder.

Definition of done:
- Builds or documents why it cannot build yet.
- Updates docs if behavior changes.
- Does not break accessibility.
